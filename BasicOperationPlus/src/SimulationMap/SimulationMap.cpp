/*
#include "SimulationMap.h"

SimulationMap::SimulationMap(uint16_t size, uint8_t scale, uint16_t posX, uint16_t posY)
{
    //TBD, random map generation would go here
}

SimulationMap::SimulationMap(const char (&map)[10][10], uint8_t scale)
{   
    uint16_t len = 10;
    for (uint16_t i = 0; i < len; i++){
        for (uint16_t j = 0; j < len; i++){
            grid[i][j] = map[i][j];
        }
    }
    this->scale = scale;
}*/